config:
  target: 'http://localhost:5000'  
  phases:
    - duration: 30  # Test duration in seconds
      arrivalRate: 1000  # Number of new users per second (simulating 50 users per second)

scenarios:
  - flow:
      - get:
          url: '/products'  # Replace with the correct endpoint
